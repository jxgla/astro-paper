# Tools quick links

The quick links section is available on both `/tools/` and `/en/tools/`. Desktop layouts use four cards per row.

The fifth card is the aggregated cloud drive search entry, so it starts the second row on desktop:

- Chinese title: `聚合网盘搜索`
- English title: `Aggregated Cloud Drive Search`
- Target: `https://pansearch.410666.xyz/`

Both page variants open the external site in a new tab.
