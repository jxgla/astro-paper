---
import DocToolPage from "@/layouts/DocToolPage.astro";
import DocConverterApp from "@/components/DocConverterApp";
---

<DocToolPage
  title="文档格式转换"
  desc="支持 md、tex、html、docx 之间的基础互转"
  heading="文档格式转换"
  subheading="先以独立子页面上线，后续再决定是否并入主 tools 页面交互。"
>
  <div class="doc-page-note">
    <div>
      <strong>当前阶段定位</strong>
      <p>这是第一版独立子页面，优先验证转换链路能否在站点环境中稳定运行，不承诺复杂排版和富文本完全保真。</p>
    </div>
    <div>
      <strong>适合转换</strong>
      <p>轻量 Markdown、LaTeX、HTML 与简单 docx 内容。复杂样式、图片、脚注、表格较多的文档建议拆分处理。</p>
    </div>
  </div>

  <DocConverterApp client:load />
</DocToolPage>

<style>
  .doc-page-note {
    display: grid;
    gap: 0.8rem;
    grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
  }

  .doc-page-note > div {
    border: 1px solid color-mix(in oklab, var(--foreground) 14%, transparent);
    border-radius: 0.9rem;
    padding: 0.9rem;
    background: color-mix(in oklab, var(--background) 96%, transparent);
  }

  .doc-page-note strong {
    display: block;
    margin-bottom: 0.35rem;
  }

  .doc-page-note p {
    margin: 0;
    font-size: 0.86rem;
    line-height: 1.6;
  }
</style>
